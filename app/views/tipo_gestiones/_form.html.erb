<%= form_for(@tipo_gestion) do |f| %>
  <% if @tipo_gestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_gestion.errors.count, "error") %> prohibited this tipo_gestion from being saved:</h2>

      <ul>
      <% @tipo_gestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
